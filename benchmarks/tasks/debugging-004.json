{
  "id": "debugging-004",
  "title": "Find the bug in a dedupe function",
  "category": "debugging",
  "difficulty": "hard",
  "prompt": "This function is supposed to remove duplicates from a list while preserving order, but sometimes returns items out of order.\n\ndef dedupe(items):\n    return list(set(items))\n\nIdentify the bug, explain the fix, and provide the corrected function in a fenced Python code block (starting with ```python), keeping the name and signature `dedupe(items)`.",
  "evaluation_type": "manual",
  "expected_output": null,
  "metadata": {
    "tags": ["python", "debugging"],
    "notes": "Bug: converting to a set discards insertion order (sets are unordered). Fix should use a seen-set with an explicit loop, or `dict.fromkeys(items)`. Revised (2026-09-23, V2.6 Phase 1) to request a fenced code block for the sandboxed grader; bug/difficulty unchanged.",
    "test_cases": [
      { "input": [[1, 2, 2, 3, 1]], "expected": [1, 2, 3] },
      { "input": [[]], "expected": [] },
      { "input": [["b", "a", "b", "c"]], "expected": ["b", "a", "c"] }
    ]
  }
}
