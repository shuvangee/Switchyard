{
  "id": "debugging-001",
  "title": "Find the bug in a list-sum function",
  "category": "debugging",
  "difficulty": "hard",
  "prompt": "The following Python function is supposed to return the sum of all numbers in a list but returns the wrong result for some inputs. Identify the bug and explain the fix.\n\ndef total(nums):\n    return sum(nums[1:])\n\nIdentify the bug, explain the fix, and provide the corrected function in a fenced Python code block (starting with ```python), keeping the name and signature `total(nums)`.",
  "evaluation_type": "manual",
  "expected_output": null,
  "metadata": {
    "tags": ["python", "debugging"],
    "notes": "Bug: nums[1:] skips the first element; should be nums[:] or nums. Revised (2026-09-23, V2.6 Phase 1) to request a fenced code block for the sandboxed grader; bug/difficulty unchanged.",
    "test_cases": [
      { "input": [[1, 2, 3]], "expected": 6 },
      { "input": [[5]], "expected": 5 },
      { "input": [[]], "expected": 0 },
      { "input": [[10, 20, 30, 40]], "expected": 100 }
    ]
  }
}
