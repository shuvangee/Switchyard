{
  "id": "debugging-003",
  "title": "Find the bug in a double_all function",
  "category": "debugging",
  "difficulty": "easy",
  "prompt": "This function should double every number in a list but returns the original list unchanged.\n\ndef double_all(nums):\n    for n in nums:\n        n = n * 2\n    return nums\n\nIdentify the bug and explain the fix.",
  "evaluation_type": "manual",
  "expected_output": null,
  "metadata": {
    "tags": ["python", "debugging"],
    "notes": "Bug: reassigning the loop variable n does not mutate the list. Fix should build a new list (e.g. `[n * 2 for n in nums]`) or index-assign `nums[i] = n * 2`. Graded manually."
  }
}
