# docs/architecture/

Architectural documentation: how Switchyard is put together and why.

- `overview.md` — current system architecture and the reasoning behind the
  major structural decisions made so far.

More documents are added here as real subsystems (routing, evaluation,
persistence) are actually built — this folder describes the system as it
exists, not as it is eventually planned to exist.
