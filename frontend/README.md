# Switchyard frontend

Next.js (App Router) + React + TypeScript. During bootstrap this is a
single status page confirming the toolchain works — no dashboards, charts,
or routing UI yet.

## Setup

```bash
cd frontend
npm install
```

## Run

```bash
npm run dev
```

Visit `http://localhost:3000`.

## Build

```bash
npm run build
```
