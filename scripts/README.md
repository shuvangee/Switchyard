# scripts/

One-off developer scripts (e.g. local setup helpers, data conversion for
benchmarks). Not application code, and not imported by `backend/` or
`frontend/`.

Empty during bootstrap — added as real scripts are actually needed.
